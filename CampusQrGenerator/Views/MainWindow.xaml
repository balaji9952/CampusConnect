<Window x:Class="CampusQrGenerator.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:CampusQrGenerator.ViewModels"
        mc:Ignorable="d"
        Title="Campus QR Code Generator - Professional Desktop Studio" 
        Height="760" Width="1200"
        WindowStartupLocation="CenterScreen"
        Background="#FFFFFF"
        FontFamily="Segoe UI, Inter, Roboto, sans-serif">

    <Window.Resources>
        <!-- Theme Color Variables -->
        <Color x:Key="PrimaryColor">#1A73E8</Color>
        <Color x:Key="PrimaryDarkColor">#0052CC</Color>
        
        <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource PrimaryColor}"/>
        <SolidColorBrush x:Key="PrimaryDarkBrush" Color="{StaticResource PrimaryDarkColor}"/>

        <SolidColorBrush x:Key="BackgroundBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="SidebarBrush" Color="#F8F9FA"/>
        <SolidColorBrush x:Key="CardBrush" Color="#FFFFFF"/>
        <SolidColorBrush x:Key="TextPrimaryBrush" Color="#202124"/>
        <SolidColorBrush x:Key="TextSecondaryBrush" Color="#5F6368"/>
        <SolidColorBrush x:Key="BorderBrush" Color="#DADCE0"/>
        <SolidColorBrush x:Key="ErrorBrush" Color="#D93025"/>
        <SolidColorBrush x:Key="SuccessBrush" Color="#1E8E3E"/>

        <!-- Custom Shadows -->
        <DropShadowEffect x:Key="CardShadow" 
                          Color="#000000" 
                          Opacity="0.08" 
                          BlurRadius="15" 
                          Direction="270" 
                          ShadowDepth="3"/>
        
        <DropShadowEffect x:Key="ButtonShadow" 
                          Color="#000000" 
                          Opacity="0.12" 
                          BlurRadius="8" 
                          Direction="270" 
                          ShadowDepth="2"/>

        <!-- Global Font Style -->
        <Style x:Key="ModernText" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Segoe UI, Inter, Roboto"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
        </Style>

        <!-- Custom Button Styles -->
        <Style x:Key="RoundedButton" TargetType="Button">
            <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="20,0"/>
            <Setter Property="Effect" Value="{StaticResource ButtonShadow}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="{StaticResource PrimaryDarkBrush}"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#DADCE0"/>
                                <Setter Property="Foreground" Value="#9AA0A6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedSecondaryButton" TargetType="Button" BasedOn="{StaticResource RoundedButton}">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#F1F3F4"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="BorderBrush" Value="#E8EAED"/>
                                <Setter Property="Foreground" Value="#DADCE0"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="RoundedDangerButton" TargetType="Button" BasedOn="{StaticResource RoundedButton}">
            <Setter Property="Background" Value="{StaticResource ErrorBrush}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                BorderThickness="0"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                              VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#B0251B"/>
                                <Setter Property="Cursor" Value="Hand"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom TextBox Style -->
        <Style x:Key="RoundedTextBox" TargetType="TextBox">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="6"
                                SnapsToDevicePixels="True">
                            <ScrollViewer x:Name="PART_ContentHost" Focusable="False" 
                                          HorizontalScrollBarVisibility="Hidden" 
                                          VerticalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#F8F9FA"/>
                                <Setter Property="Foreground" Value="#9AA0A6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Custom ComboBox Style -->
        <Style x:Key="RoundedComboBox" TargetType="ComboBox">
            <Setter Property="Background" Value="{StaticResource CardBrush}"/>
            <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ComboBox">
                        <Grid x:Name="templateRoot" SnapsToDevicePixels="True">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition MinWidth="34" Width="0"/>
                            </Grid.ColumnDefinitions>
                            <ToggleButton x:Name="toggleButton" 
                                          BorderBrush="{TemplateBinding BorderBrush}" 
                                          BorderThickness="{TemplateBinding BorderThickness}" 
                                          Background="{TemplateBinding Background}" 
                                          Grid.ColumnSpan="2" 
                                          IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}">
                                <ToggleButton.Template>
                                    <ControlTemplate TargetType="ToggleButton">
                                        <Border x:Name="templateBorder" 
                                                Background="{TemplateBinding Background}" 
                                                BorderBrush="{TemplateBinding BorderBrush}" 
                                                BorderThickness="{TemplateBinding BorderThickness}" 
                                                CornerRadius="6">
                                            <Grid HorizontalAlignment="Right" Width="30" Height="20">
                                                <Path x:Name="arrow" 
                                                      Data="M 0 0 L 4 4 L 8 0 Z" 
                                                      Fill="{StaticResource TextSecondaryBrush}" 
                                                      HorizontalAlignment="Center" 
                                                      VerticalAlignment="Center"/>
                                            </Grid>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="true">
                                                <Setter TargetName="templateBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                            </Trigger>
                                            <Trigger Property="IsChecked" Value="true">
                                                <Setter TargetName="templateBorder" Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </ToggleButton.Template>
                            </ToggleButton>
                            <ContentPresenter x:Name="contentPresenter" 
                                              ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" 
                                              Content="{TemplateBinding SelectionBoxItem}" 
                                              ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" 
                                              HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" 
                                              IsHitTestVisible="False" 
                                              Margin="{TemplateBinding Padding}" 
                                              SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" 
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                            <Popup x:Name="PART_Popup" 
                                   AllowsTransparency="True" 
                                   Grid.ColumnSpan="2" 
                                   IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" 
                                   Placement="Bottom">
                                <Border x:Name="dropDownBorder" 
                                        Background="{StaticResource CardBrush}" 
                                        BorderBrush="{StaticResource BorderBrush}" 
                                        BorderThickness="1" 
                                        CornerRadius="6" 
                                        MaxHeight="{TemplateBinding MaxDropDownHeight}" 
                                        MinWidth="{Binding ActualWidth, ElementName=templateRoot}">
                                    <ScrollViewer x:Name="TemplatesScrollViewer" SnapsToDevicePixels="True">
                                        <ItemsPresenter x:Name="ItemsPresenter" 
                                                        KeyboardNavigation.DirectionalNavigation="Contained" 
                                                        SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                    </ScrollViewer>
                                </Border>
                            </Popup>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="HasItems" Value="False">
                                <Setter TargetName="dropDownBorder" Property="Height" Value="95"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="#9AA0A6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid.ColumnDefinitions>
            <!-- Left Sidebar Form -->
            <ColumnDefinition Width="420"/>
            <!-- Right Preview & Card Area -->
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Left Sidebar: Controls & Input Form -->
        <Border Grid.Column="0" Background="{StaticResource SidebarBrush}" BorderBrush="{StaticResource BorderBrush}" BorderThickness="0,0,1,0" Padding="24">
            <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                <StackPanel>
                    <!-- App Header -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,24">
                        <Border Background="{StaticResource PrimaryBrush}" Width="40" Height="40" CornerRadius="8" Margin="0,0,12,0">
                            <TextBlock Text="QR" Foreground="White" FontWeight="Bold" FontSize="18" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <StackPanel VerticalAlignment="Center">
                            <TextBlock Text="Campus QR Studio" Style="{StaticResource ModernText}" FontWeight="Bold" FontSize="20"/>
                            <TextBlock Text="Offline Administration Tool" Style="{StaticResource ModernText}" Foreground="{StaticResource TextSecondaryBrush}" FontSize="12"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Connection Status and Sync Widget -->
                    <Border CornerRadius="8" Background="#F1F3F4" Padding="12,10" Margin="0,0,0,18">
                        <StackPanel>
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding ConnectionStatusText}" Style="{StaticResource ModernText}" FontWeight="SemiBold" FontSize="13" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding PendingSyncCount, StringFormat='{}{0} pending'}" FontSize="12" FontWeight="SemiBold" Foreground="{StaticResource ErrorBrush}" VerticalAlignment="Center" Margin="8,0,0,0">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock" BasedOn="{StaticResource ModernText}">
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding PendingSyncCount}" Value="0">
                                                    <Setter Property="Visibility" Value="Collapsed"/>
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                            </Grid>
                            <Button Content="RETRY OFFLINE SYNC" 
                                    Command="{Binding RetrySyncCommand}" 
                                    Height="32" Margin="0,8,0,0" FontSize="11">
                                <Button.Style>
                                    <Style TargetType="Button" BasedOn="{StaticResource RoundedDangerButton}">
                                        <Setter Property="Visibility" Value="Collapsed"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding ShowRetryButton}" Value="True">
                                                <Setter Property="Visibility" Value="Visible"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Button.Style>
                            </Button>
                        </StackPanel>
                    </Border>

                    <!-- QR Number Field -->
                    <TextBlock Text="QR NUMBER / ID" Style="{StaticResource ModernText}" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding QrNumber, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RoundedTextBox}" Margin="0,0,0,16"/>

                    <!-- Location Field -->
                    <TextBlock Text="LOCATION NAME" Style="{StaticResource ModernText}" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding Location, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RoundedTextBox}" Margin="0,0,0,16"/>

                    <!-- Internal Code Field -->
                    <TextBlock Text="INTERNAL CODE (OPTIONAL)" Style="{StaticResource ModernText}" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <Grid Margin="0,0,0,16">
                        <TextBox x:Name="InternalCodeBox" Text="{Binding InternalCode, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RoundedTextBox}"/>
                        <TextBlock Text="e.g. LIB-01, MESS-A" IsHitTestVisible="False" Foreground="#9AA0A6" FontSize="14" Margin="14,10,12,10" VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                    <Style.Triggers>
                                        <MultiDataTrigger>
                                            <MultiDataTrigger.Conditions>
                                                <Condition Binding="{Binding Text, ElementName=InternalCodeBox}" Value=""/>
                                                <Condition Binding="{Binding IsFocused, ElementName=InternalCodeBox}" Value="False"/>
                                            </MultiDataTrigger.Conditions>
                                            <Setter Property="Visibility" Value="Visible"/>
                                        </MultiDataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Grid>


                    <!-- Floor Selection -->
                    <TextBlock Text="FLOOR LEVEL" Style="{StaticResource ModernText}" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <ComboBox ItemsSource="{Binding Floors}" SelectedItem="{Binding SelectedFloor}" Style="{StaticResource RoundedComboBox}" Margin="0,0,0,16"/>

                    <!-- Category Selection -->
                    <TextBlock Text="CATEGORY" Style="{StaticResource ModernText}" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <ComboBox ItemsSource="{Binding Categories}" SelectedItem="{Binding SelectedCategory}" Style="{StaticResource RoundedComboBox}" Margin="0,0,0,16"/>

                    <!-- Department Selection -->
                    <TextBlock Text="DEPARTMENT" Style="{StaticResource ModernText}" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <ComboBox ItemsSource="{Binding Departments}" SelectedItem="{Binding SelectedDepartment}" Style="{StaticResource RoundedComboBox}" Margin="0,0,0,16"/>

                    <!-- Status Selection -->
                    <TextBlock Text="STATUS" Style="{StaticResource ModernText}" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <ComboBox ItemsSource="{Binding StatusOptions}" SelectedItem="{Binding SelectedStatus}" Style="{StaticResource RoundedComboBox}" Margin="0,0,0,16"/>

                    <!-- Routing Type Selection -->
                    <TextBlock Text="ROUTING TYPE" Style="{StaticResource ModernText}" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <ComboBox ItemsSource="{Binding RoutingTypes}" SelectedItem="{Binding SelectedRoutingType}" Style="{StaticResource RoundedComboBox}" Margin="0,0,0,16"/>

                    <!-- Base URL / Scan Target -->
                    <TextBlock Text="SCAN TARGET BASE URL (OPTIONAL)" Style="{StaticResource ModernText}" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,6"/>
                    <TextBox Text="{Binding BaseUrl, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RoundedTextBox}" Margin="0,0,0,24"/>

                    <!-- Generate Action -->
                    <Button Content="GENERATE QR CODE" Command="{Binding GenerateCommand}" Style="{StaticResource RoundedButton}" Margin="0,0,0,12"/>
                    <Button Content="CLEAR FORM" Command="{Binding ClearCommand}" Style="{StaticResource RoundedSecondaryButton}" Margin="0,0,0,24"/>

                    <Separator Margin="0,0,0,24" Background="{StaticResource BorderBrush}"/>

                    <!-- Export Actions Section -->
                    <TextBlock Text="EXPORT &amp; PRINT" Style="{StaticResource ModernText}" FontWeight="Bold" FontSize="14" Margin="0,0,0,12"/>
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="10"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Button Grid.Column="0" Content="SAVE PNG" Command="{Binding SavePngCommand}" Style="{StaticResource RoundedSecondaryButton}"/>
                        <Button Grid.Column="2" Content="EXPORT PDF" Command="{Binding ExportPdfCommand}" Style="{StaticResource RoundedSecondaryButton}"/>
                    </Grid>
                    <Button Content="PRINT CARD DIRECTLY" Command="{Binding PrintCommand}" Style="{StaticResource RoundedButton}"/>
                    
                    <Separator Margin="0,20,0,20" Background="{StaticResource BorderBrush}"/>

                    <!-- Server settings -->
                    <Expander Header="SERVER CONNECTION SETTINGS" 
                              Background="Transparent" 
                              BorderThickness="0" 
                              Foreground="{StaticResource TextSecondaryBrush}"
                              FontWeight="SemiBold" 
                              FontSize="12"
                              Margin="0,0,0,16">
                        <StackPanel Margin="0,10,0,0">
                            <!-- Local URL -->
                            <TextBlock Text="LOCAL SERVER URL" Style="{StaticResource ModernText}" FontWeight="SemiBold" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding LocalUrlSetting, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RoundedTextBox}" Margin="0,0,0,12"/>

                            <!-- ngrok URL -->
                            <TextBlock Text="NGROK PUBLIC URL" Style="{StaticResource ModernText}" FontWeight="SemiBold" FontSize="11" Foreground="{StaticResource TextSecondaryBrush}" Margin="0,0,0,4"/>
                            <TextBox Text="{Binding NgrokUrlSetting, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource RoundedTextBox}" Margin="0,0,0,16"/>

                            <Button Content="SAVE &amp; TEST CONNECTION" Command="{Binding SaveSettingsCommand}" Style="{StaticResource RoundedButton}" Height="36" FontSize="12"/>
                        </StackPanel>
                    </Expander>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Right Area: Live Printable Card Preview -->
        <Grid Grid.Column="1" Background="{StaticResource BackgroundBrush}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <TextBlock Text="LIVE HIGH-RESOLUTION PRINT PREVIEW (300 DPI CARD)" Style="{StaticResource ModernText}" FontWeight="Bold" FontSize="13" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,0,0,20"/>
                
                <!-- Card Container (Targeted by MainWindow.xaml.cs RenderCardVisual) -->
                <Border x:Name="CardVisualBorder" 
                        Width="380" Height="570" 
                        Background="White" 
                        CornerRadius="16" 
                        BorderBrush="{StaticResource BorderBrush}" 
                        BorderThickness="1" 
                        Effect="{StaticResource CardShadow}" 
                        RenderOptions.BitmapScalingMode="HighQuality"
                        Padding="28">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>



                        <!-- Middle Area: QR Code Image & Location details -->
                        <StackPanel Grid.Row="1" VerticalAlignment="Center" HorizontalAlignment="Center">
                            <Border Width="240" Height="240" Background="#F8F9FA" BorderBrush="#E8EAED" BorderThickness="1" CornerRadius="12" Padding="12" Margin="0,0,0,20">
                                <Image Source="{Binding QrCodeImage}" Stretch="Uniform" HorizontalAlignment="Center" VerticalAlignment="Center" RenderOptions.BitmapScalingMode="HighQuality"/>
                            </Border>

                            <TextBlock Text="{Binding Location}" Style="{StaticResource ModernText}" FontWeight="Bold" FontSize="22" Foreground="#202124" HorizontalAlignment="Center" TextWrapping="Wrap" TextAlignment="Center" MaxWidth="320"/>
                            <TextBlock Text="{Binding SelectedFloor}" Style="{StaticResource ModernText}" FontWeight="Normal" FontSize="14" Foreground="{StaticResource TextSecondaryBrush}" HorizontalAlignment="Center" Margin="0,6,0,0"/>
                        </StackPanel>

                        <!-- Footer Area: QR ID & Instructions -->
                        <Border Grid.Row="2" Background="#F8F9FA" CornerRadius="8" Padding="12,10" Margin="0,16,0,0">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="LOCATION CODE:" Style="{StaticResource ModernText}" FontWeight="SemiBold" FontSize="12" Foreground="{StaticResource TextSecondaryBrush}" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding QrNumber}" Style="{StaticResource ModernText}" FontWeight="Bold" FontSize="14" Foreground="#202124" VerticalAlignment="Center"/>
                            </Grid>
                        </Border>
                    </Grid>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
